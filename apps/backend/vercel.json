{
  "version": 2,
  "buildCommand": "echo 'Backend ready for deployment'",
  "env": {
    "NODE_ENV": "production"
  },
  "builds": [
    {
      "src": "src/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.js"
    }
  ]
}
